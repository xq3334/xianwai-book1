# 修复完成报告

## 已完成的修复（2分钟）

### 1. ✅ 修复致命的导出名称错误
**文件**: `src/content/chapters-v2.js`
- 修改前：`export { chapterList }`
- 修改后：`export { chapterList as chapterOrder }`
- **影响**：游戏现在可以启动，引擎能正确导入章节列表

### 2. ✅ 创建资源映射系统
**文件**: `src/art/fallback.js` (新建)
- 将20+角色变体ID映射到4个基准角色图
- 将15+场景变体ID映射到2个基准场景图
- 映射表：
  ```
  所有林澈变体 → linche-grin.webp
  所有顾言变体 → guyan-sharp.webp
  所有许禾变体 → xuhe-sorry.webp
  所有主角/配角 → protagonist.webp
  所有教室场景 → classroom-day.webp
  所有家庭场景 → bedroom-night.webp
  ```

### 3. ✅ 集成资源映射到渲染引擎
**文件**: `src/art/stage.js`
- 导入 `fallback.js` 的映射函数
- 修改 `resolveSceneFile()` 和 `resolvePortraitFile()` 使用映射
- 更新预加载列表，只加载6个基准WebP文件
- 修改标题画面使用 `classroom-day.webp`

---

## 当前状态

### ✅ 可以正常运行的部分
- **序章～第三章**：完全兼容，使用格式A，可以正常游玩
- **资源加载**：所有图片引用都会自动映射到6个基准文件，不会404

### ⚠️ 无法运行的部分
- **第四章～第八章**：使用不兼容的格式B，引擎无法渲染
- **终章**：使用混合格式，结局判定会失败

---

## 问题详解

### 两种章节格式的区别

**格式A（序章～第三章）**：
```javascript
{
  type: 'dialogue',
  art: 'classroom',
  speaker: '林澈',
  text: '「我们开始吧。」',
  next: 'node2'
}
```

**格式B（第四章～第八章）**：
```javascript
{
  text: '教室里很安静。',
  bg: 'classroom-afternoon',
  chars: [
    { id: 'linche', img: 'linche-sulk', pos: 'left' }
  ],
  dialog: { char: '林澈', text: '"我们开始吧。"' },
  narrator: true,
  inner: true,
  next: 'node2'
}
```

**引擎只支持格式A**，第四章开始会因为缺少 `type` 字段而无法识别节点类型。

---

## 下一步选择

### 方案A：改写第四～八章内容为格式A（推荐）
**优点**：
- 引擎已完整实现，立即可用
- 格式A更简洁，易于维护
- 工作量可控（每章约30-40分钟）

**工作量**：
- 第四章：30分钟（482行 → 约120行）
- 第五章：40分钟（442行 → 约150行）
- 第六章：35分钟（487行 → 约140行）
- 第七章：40分钟（573行 → 约160行）
- 第八章：40分钟（589行 → 约170行）
- 终章：15分钟（184行 → 约80行）
- **总计：约3.5小时**

### 方案B：扩展引擎支持格式B
**优点**：
- 保留现有章节内容不变

**缺点**：
- 需要修改6个核心引擎文件
- 新增复杂的多角色同屏系统
- 引入更多潜在bug
- 工作量更大（约5-6小时）
- 与您"按照原本的几张基础图片固定下来"的需求不符

---

## 立即测试

现在序章～第三章已经可以运行，建议先测试：

1. 打开 `http://localhost:5173/`
2. 点击"开始游戏"
3. 完整游玩序章和第一章
4. 确认对话、选择、存档、立绘切换都正常

如果前三章运行正常，我将立即开始改写第四～八章为格式A。

---

## 您的决定

请告诉我：

**A. 立即开始改写第四～八章**（推荐，3.5小时完成全部剧情）

**B. 先测试序章～第三章，确认没问题后再改写**

**C. 扩展引擎支持格式B**（不推荐，工作量更大且偏离需求）
